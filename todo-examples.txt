KRILL TODO ROADMAP

Priority legend:
P0 = foundational / unblockers
P1 = high-impact musical features
P2 = exploratory / advanced

==================================================
P0 - Core correctness and architecture hygiene
==================================================

[ ] Constant-folding of render nodes
    - If a node's children are constant, replace node with rendered value when possible.

[ ] Normalize factory argument building
    - Call buildTreeForNode on every argument.
    - Pass arguments as arrays and assert expected arity.

[ ] Stack reliability and tests
    - Add targeted test cases for stack behavior.
    - Ensure this works correctly:
      stack [ "hh hh", fast 2 $ "bd sd" ]

==================================================
P1 - Existing feature ideas (from current TODO)
==================================================

[ ] Accent / ghost tokens in mininotation
    - Implement + and - style tokens (exact syntax to finalize).

[ ] Free JavaScript transform hook
    - Allow a user-provided JS function to transform a pattern.

[ ] Stack as grouping element for parallel processing
    - Goal: parallel sequences with operators.
    - Example:
      stack [ slow 4 $ "hh hh", slow 2 $ "bd sd" ]

[ ] Add pattern operator improvements
    - Goal: octave-shift style behavior similar to Norns Awake workflows.
    - Example:
      stack [ add 3, add 5 ] $ "c3"

[ ] Shift as a pattern modifier
    - Example direction:
      "[bd [sd bd] [~ sd] bd]%4" -> "[bd [sd bd] [sd]>0.5 bd]%4"

==================================================
P1 - Tidal / Strudel inspired workflow features
==================================================

[ ] Multi-stream live coding model
    - d1/d2/d3 style independent streams.
    - Include mute, solo, hush, panic, per-stream replace.

[ ] Higher-order periodic transforms
    - every / whenmod style transforms on cycle counts.
    - jux/off style parallel-variation transforms.

[ ] Deterministic randomness toolkit
    - choose / sometimes / degrade / rand / irand style operators.
    - Explicit seed control for reproducible performance and tests.

[ ] Pattern-of-parameters everywhere
    - Operator args can be constants or patterns.
    - Apply to timing, pitch transforms, velocity-like metadata.

[ ] Richer mininotation ergonomics
    - Better shorthand for alternation, repetition, and polymeter-like phrasing.

==================================================
P1 - Transition tools (note-event only context)
==================================================

[ ] Event-level xfade (non-audio)
    - Implement crossfade as note-event blending, not instrument gain fade.
    - Transition window measured in cycles.
    - Deterministic weighted selection between pattern A and B over time.
    - Define collision policy when both patterns emit notes at same timestamp.

[ ] Scene and replacement transitions
    - Queue next pattern at cycle boundary.
    - Optional smooth replace modes (hard switch vs blend window).

==================================================
P2 - Performance operations and observability
==================================================

[ ] Timing diagnostics
    - Jitter/latency counters and late-event metrics.

[ ] Lightweight event inspector
    - Inspect current cycle query output and decision path.

[ ] Control lanes / macro lanes
    - Separate note lane from control-modulation lanes.
    - Mapping presets for hardware controllers.

==================================================
Reference examples to revisit
==================================================

- Play sequence every N bars:
  d1 $ every' 8 0 (const (s "cc")) $ silence

- Reich-style drift:
  d1 $ jux (slow 1.005) $ s "its_gonna_rain"
